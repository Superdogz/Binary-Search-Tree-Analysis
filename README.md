# Binary-Search-Tree-Analysis
Standard implementation of a binary search tree. Functions include insertion, find_min, and tree traversal.
Also shows the runtime of the insert and find_min functions in the form of a graph.
